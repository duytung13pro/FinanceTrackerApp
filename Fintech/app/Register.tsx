import { StyleSheet } from "react-native";

export default function Register() {
    return <h1 style={style.editThis}>Register</h1>;
}

const style = StyleSheet.create({
    editThis: {
        color: "orange",
    },
});
